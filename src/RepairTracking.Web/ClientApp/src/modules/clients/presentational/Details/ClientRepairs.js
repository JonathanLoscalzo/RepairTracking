import React from 'react'
import ReactTable from 'react-table';

const ClientRepairs = (props) => {
  let {repairs} = props;
  return (
    /*<div>
        <Col md={{ size: 7, offset: 3 }} style={{marginLeft:"350px"}}>
            <ReactTable
                {...props}
                defaultPageSize={10}
                className="-striped -highlight"
                data={data}
                columns={columns}
                previousText="Anterior"
                nextText="Siguiente"
                showPageSizeOptions= {false}
                pageText= "Página"
                ofText= "de"
                defaultSorted={[
                    {
                    id: "code",
                    asc: true
                }
                ]}
            />
        </Col>
    </div>*/
    <div>hola</div>
  )
}

export default ClientRepairs
